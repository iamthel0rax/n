var Err = require('utils/error');
var Defaults = require('utils/defaults');


var _defaults = {
	tileboardClass: 'tileboard',
	tileClass: 'tile',
	activeClass: 'active'
	speed: 2500,
};
var TileBoard = function(options) {
	
	
}

module.exports = TileBoard; 